-- first line because it is first line in that module
-- columns 24 and 43 because error is on parent name
-- expect symbol errors: 1:24
import Other

-- module: Other
module Other exposing (Union(Boo, Bar))

type Union =
    | Foo
    | Bar
